import 'package:flutter/material.dart';

class RecommendedDone extends StatelessWidget {
  const RecommendedDone({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('추천 완결'),
      ),
    );
  }
}
