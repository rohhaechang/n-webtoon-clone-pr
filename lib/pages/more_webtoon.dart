import 'package:flutter/material.dart';

class MoreWebtoon extends StatelessWidget {
  const MoreWebtoon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('더보기'),
      ),
    );
  }
}
